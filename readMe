1.概述
fzk模板工程，用于开发过程中常用代码的模板复制和代码生成，提高开发效率
2.快速使用入门
（1）在/resources/template/目录下进行目标模板编写
（2）/resources/param/目录下下进行与目标模板变量名称一一对应的属性文件配置
（例如模板代码中存在${var1},则属性文件中必须有var1=...的配置）
（3）自动生成目标操作类相关目录文件，放入对应的目录位置；
（4）微调操作类，使符合需求；
（5）调用main方法，生成目标文件，在/resources/target/目录下即可取出。
3.注意事项
（1）目录规范说明1：
resources目录下最多只能有2层子目录，例如/template/js/,在js目录下不允许再创建子目录，
所有与js模块相关的模板文件放在该目录下即可，没有必要再细分。
如不遵守该约定，运行时将出找不到路径相关错误！
（2）

